<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Gerald test page</title>

    <!-- Bootstrap -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            padding-top: 50px;
        }
        .starter-template {
            padding: 40px 15px;
            text-align: center;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                        aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">Gerald</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="starter-template">
            <h1>Gerald Test forms</h1>
        </div>
        <div class="row">
            <div class="col-md-4">
                <h2>Create</h2>
                <form method="post" action="{{ path('document.rest.create') }}" enctype="multipart/form-data">
                    <input type="file" name="file" id="file">
                    <p>JSON meta:</p>
                    <textarea title="meta" cols=40 rows=10 name="meta"></textarea>
                    <button type="submit">Go</button>
                </form>
            </div>
            <div class="col-md-4">
                <h2>Update</h2>
                <p>Not yet working</p>
                <form method="post" action="{{ path('document.rest.create') }}">
                    <input type="file" name="file" id="file">
                    <p>JSON meta:</p>
                    <textarea title="meta" cols=40 rows=10 name="meta"></textarea>
                </form>
            </div>
        </div>
    </div><!-- /.container -->

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</body>
</html>
